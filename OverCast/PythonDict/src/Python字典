字典：
    字典是元素呈键值对的形式，以逗号分隔，无序的，可以修改的序列。
    字典是Python基础数据类型当中唯一一个映射关系的数据类型，通常对应json
    在Python当中我们将json叫做类字典格式，在json当中，我们把字典叫做类json格式

    定义：
    zip函数           将几个序列的对应的索引位上的元素分到一个元组当中，形成一个大元组，子元组的格式取决与最短序列的长度
    dict             似的
    fromkeys         以后的序列的元素为键，值为None的字典

    字典的特点：
    因为字典是无序的，所以字典没有索引值
    因为字典没有索引值，所以字典以键值对，（字典的键相当与列表的值的索引值）
    因为字典以键值对，所以字典键唯一且不可修改
    因为字典的键不可修改，所以列表和字典不可以给字典做键

    字典的方法：

    字典的取值
    keys            获取字典所有的键
    values          获取字典所有的值
    get             以键取值，如果指定键不存在，默认返回None，可以指定返回内容
    update          更新制定键的内容，如果键不存在，创建
    setdefault      设置默认，如果键存在，返回值，如果键不存在，创建键，默认为None，值也可以自定义
    items           返回字典键值，呈元组形式的格式