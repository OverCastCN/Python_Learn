字符串的变形：
upper               将字符串的所有字母转换为大写
lower               将字符串的所有字母转换为小写
swapcase            将字符串的所有字母转换为大小写互换
title               将每个“单词”的首字母大写，以空格识别
excpadntabs         修改\t的长度（该方法不常用）
capitalize          字符串的首字母大写

字符串的判断：
isalnum             判断字符串是否完全由字母和数字组成
isalpha             判断字符串是否完全由字母组成
isdigit             判断字符串是否完全由数字组成
isupper             判断字符串是否完全大写字母组成
islower             判断字符串是否完全小写字母组成
istitle             判断字符串是否完满足title格式
isspace             判断字符串是否由空格组成
startwith           判断字符串是否由指定的字符开头，也可以截取字符
endwith             判断字符串是否由指定字符串结尾，也可以截取字符