字符串的切分：
splitlines          用行来切分字符串，可以指定保留换行符，0和1表示布尔值
split               默认以空格从左开始切分字符串，可以指定切分多少次
rsplit              默认以空格从右开始切分字符串，可以指定切分多少次

字符串的拼接：
join                指定字符串插入到后面徐磊，每两个元素之间插入
*                   将字符串重复指定次数
+                   拼接两个字符串