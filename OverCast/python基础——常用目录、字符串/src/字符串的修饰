Center      让字符串在指定长度居中，若不能居中，左短右长，可以指定填充的内容，默认为空格
ljust       让字符穿在指定的长度左对齐
rjust       让字符串在指定的长度右对齐
zifill      让字符串填充到指定长度，不足的地方默认使用0填充，从左开始填充
format      按照顺序将后面的参数传递给前面的大括号
strip       默认去除字符串两端的空格
lstrip      默认去除字符串左端的空格
rstrip      默认去除字符串右端的空格